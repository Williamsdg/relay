<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Chromium compiles JavaScript at runtime, so the hardened runtime has to
       permit JIT and the writable-executable pages V8 relies on. Without
       these the app is signed but crashes the moment a page loads. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Reaching Xbox Live and the streaming host. -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Party chat sends the microphone to the console. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
